# 静态资源 - 确保这些路径不会被重定向
/assets/* /assets/:splat 200
/css/* /css/:splat 200
/js/* /js/:splat 200
/fonts/* /fonts/:splat 200
/icons/* /icons/:splat 200

# API代理
/api/* https://betterdeal-worker1.wenmoon124.workers.dev/:splat 301

# 允许Cloudflare资源
/cdn-cgi/* /_cdn-cgi/:splat 200

# 正确处理字体资源
/*.woff /assets/:splat 200
/*.woff2 /assets/:splat 200

# 强制HTTPS
http://*  https://:splat  301

# SPA应用程序 fallback - 放在最后，避免循环
/* /index.html 200!